require 'rails_helper'

RSpec.describe Piece, type: :model do
  describe "is_horizontally_obstructed?" do
    it "should raise an error if squares aren't vertically aligned" do

    end

    it "should raise an error if squares are adjacent" do
    end

    it "should raise an error if squares are the same" do
    end

    it "should return true if a piece is between them" do
    end

    it "should return false if a piece isn't between them" do
    end
  end
end
