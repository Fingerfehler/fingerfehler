<%= link_to 'Lets Play!', new_game_path %>

<br>

<h3>Games List</h3>

<% @games.each do |game| %>
  <% if @currentUser == game.white_player_id %>
    <h1>Works</h1>
    <br>
  <% else %>
    <%= link_to game.name, game_path(game) %>
    <br>
  <% end %> 
<% end %>